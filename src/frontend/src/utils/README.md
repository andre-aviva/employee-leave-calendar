# utils

Shared frontend utilities. `formatDate.ts` is the single conversion point between wire format (ISO YYYY-MM-DD) and UI format (DD-MM-YYYY). No component renders or accepts a raw ISO string directly.
