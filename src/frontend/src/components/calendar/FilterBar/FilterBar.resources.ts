export type FilterBarResources = {
  searchLabel: string;
  searchPlaceholder: string;
  typeLabel: string;
  typePlaceholder: string;
  allTypes: string;
};

export const resources: FilterBarResources = {
  searchLabel: 'Employee',
  searchPlaceholder: 'Search by name...',
  typeLabel: 'Leave Type',
  typePlaceholder: 'All types',
  allTypes: 'All types',
};
